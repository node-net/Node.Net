using NUnit.Framework;
using System.Threading;
using System.Windows;

namespace Node.Net.Controls
{/*
    [TestFixture]
    class ContentControlTest
    {
        [Test, Apartment(ApartmentState.STA), Explicit]
        public void ContentControl_ShowDialog()
        {
            var properties = new Xceed.Wpf.Toolkit.PropertyGrid.PropertyGrid
            {
                SelectedObject = new Node.Net.Controls.ContentControl { Name = "test" }
            };
            new Window
            {
                Title = nameof(ContentControl_ShowDialog),
                WindowState = WindowState.Maximized,
                Content = new Xceed.Wpf.Toolkit.PropertyGrid.PropertyGrid
                {
                    SelectedObject = new Node.Net.Controls.ContentControl()
                }
            }.ShowDialog();
        }
    }*/
}
