using NUnit.Framework;
using System.Threading;
using System.Windows.Media;

namespace Node.Net.Factory.Test.Factories.TypeFactories
{
    class ImageSourceFactoryTest
    {
        /*
        [Test, Apartment(ApartmentState.STA)]
        [TestCase("Mesh.Cube.xaml")]
        public void ImageSourceFactory_Create(string resourceName)
        {
            var source = GlobalFixture.GetResource(resourceName);
            Assert.IsNotNull(source);
            var factory = new Node.Net.Factory.Factories.TypeFactories.ImageSourceFactory();
            Assert.NotNull(factory.Create<ImageSource>(source));
        }*/
    }
}
