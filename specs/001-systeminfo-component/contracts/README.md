# Contracts Directory

This directory would contain API contracts if the feature required them.

For the SystemInfo Razor component, no API contracts are needed as it is a client-side Blazor component with no external API dependencies.
